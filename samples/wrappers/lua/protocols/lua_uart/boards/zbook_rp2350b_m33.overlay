/*
 * Point zbook-uart at uart0 (console wire) so this sample's output is
 * visible in the same terminal as the shell/log, instead of uart1 which
 * is wired to the wifi shield on P20/P21.
 */

/ {
	aliases {
		zbook-uart = &uart0;
	};
};
